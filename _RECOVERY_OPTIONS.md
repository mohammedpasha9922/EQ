# EQ7 — تقرير تحديد نسخة الاسترجاع (قراءة فقط، لم يتم تعديل أي ملف)

التاريخ: 2026-09-22 · المشروع: `D:\Programs EQ7\EQ` (PWA / JavaScript)

---

## 1) الخط الزمني للـ Checkpoints (اليوم 2026-09-22)

الـ backups مقروءة من `refs/cline/checkpoints/<session>/<run>` — إجمالي **573** checkpoint، منها **17** اليوم.
كل checkpoint هو snapshot **كامل** لشجرة العمل (بما فيها untracked: `tests/`, `src/`, `__notes_test/`, `fonts/`).

| الوقت | Ref | index.html | app.js | styles.css | الحالة |
|---|---|---|---|---|---|
| 15:32:31 | `1790067360119_xnrem/2` = `95b5725` | 108,933 B · `37a121715b32` | 824,299 B · `8443b859ba31` | 182,075 B | ✅ نظيف |
| 16:04:17 | `1790082243226_8vhp5/1` = `bfe7fbc` | 108,933 B · `37a121715b32` | 824,299 B · `8443b859ba31` | 182,075 B | ✅ نظيف (مطابق) |
| 16:15:06 | `1790082243226_8vhp5/2` = `218a48f` | 108,933 B · `37a121715b32` | 824,299 B · `8443b859ba31` | 182,075 B | ✅ نظيف (مطابق) |
| **16:20:33** | **`1790082243226_8vhp5/3` = `259d1e1`** | **108,933 B · `37a121715b32`** | **824,299 B · `8443b859ba31`** | **182,075 B** | **✅ آخر حالة نظيفة** |
| 16:32:23 | `…8vhp5/4` = `b3378c0` | 108,499 B · **U+FFFD=88 · arabic=0 · `???`=21** | 824,233 B | 182,075 B | ❌ تلف encoding |
| 16:37:53 | `…8vhp5/5` = `ad01dcb` | 108,499 B · **U+FFFD=88** | 824,233 B | 182,075 B | ❌ نفس التلف |
| 16:56:17 | `…8vhp5/6` = `d192e88` | 89,950 B (base) | 534,176 B | 182,075 B | ❌ تراجع للأساس |
| 17:49:01 | `…8vhp5/7` = `bb0de30` | 89,950 B (base) | 534,176 B | 182,075 B | ❌ تراجع |
| 17:50:54 | `…8vhp5/8` = `5e2e67d` | 89,950 B (base) | 534,176 B | 182,075 B | ❌ تراجع (= محتوى HEAD) |
| **الآن (disk)** | — | **91,295 B** | **546,139 B** | **190,072 B** | ❌ |

### القراءة التشخيصية

1. **الحالة النظيفة (`259d1e1`، 16:20:33) مؤكَّدة 4 مرات** — نفس الـSHA بالضبط في `xnrem/2` + `8vhp5/1` + `8vhp5/2` + `8vhp5/3`.
2. **`???` ظهرت في `8vhp5/4` (16:32:23)** — `U+FFFD=88`, `arabic=0`. الـindex.html فقد كل العربي في هذه اللقطة.
3. **التراجع الحقيقي بدأ في `8vhp5/6` (16:56:17)** — عادت الملفات لحجم الأساس.
4. **الحالة الحالية (disk) أسوأ من النظيفة**: `app.js` = 546 KB فقط، وفيه:
   * `smartPdf` = **0** (كان 797)
   * `pdfV1` = **0** (كان 28)
   * `id="pdfV1Tools"` = **0**، `id="smartPdfToolbar"` = **0**
   * `adBar` = **0** (كان 8)
   → أي أن **محرّك Smart PDF / Smart Documents بكامله (~280 KB كود) مفقود من الملف الحي**، وواجهته مفقودة من `index.html`.
5. ملفات `C:\EQ_RECOVERY_20260922_*` كلها تحتوي **نفس الحالة المتضررة** (index.html 91,295 B / app.js 546,139 B) — لذلك الاستعادة التي جرت منها لم تُصلح شيئًا.

---

## 2) الخيارات المتاحة

### الخيار A — `259d1e1` (الموصى به) ✅

**`refs/cline/checkpoints/1790082243226_8vhp5/3`** — 2026-09-22 16:20:33

| ملف | الحجم | U+FFFD | عربي | BOM |
|---|---|---|---|---|
| index.html | 108,933 B | 0 | 66 | ✅ نعم (UTF-8 BOM = الصحيح) |
| app.js | 824,299 B | 0 | 20,448 | لا |
| styles.css | 182,075 B | 0 | 207 | لا |

* `node --check` على `app.js` → **exit=0** (صيغة سليمة).
* يحتوي: `id="pdfV1Tools"`=1 · `pdfV1DelBtn`=1 · `id="smartPdfToolbar"`=1 · `smartPdf`=21 · `adBar` · `currency`=137 · `calculator`=13 · `notes`=58 · `signature`=4.
* **يعني حرفيًا: حالة ما قبل مهمة حذف `pdfV1DelBtn`** (الزر موجود) و**قبل** ظهور `???`.
* نسخة كاملة 707 ملف (بدون `node_modules`): يشمل `tests/`, `src/`, `__notes_test/`, `fonts/`.

**الميزة:** آخر نقطة نظيفة قبل الكارثة، ومتطابقة مع 3 لقطات أخرى.
**الحد:** لا يشمل أي شيء عملته بعد 16:20:33 (لا يوجد شيء سليم بعدها أصلاً).

---

### الخيار B — `95b5725` / `218a48f` (بديل مكافئ تقريبًا)

`xnrem/2` (15:32) أو `8vhp5/2` (16:15) — **بنفس SHA** لـ index.html/app.js/styles.css بالضبط.
لا فرق فعلي عن الخيار A. لا سبب لتفضيله.

---

### الخيار C — `_pristine/` (2026-09-17 12:34)

| ملف | الحجم |
|---|---|
| index.html | 163,034 B (`871997642c60`) |
| app.js | 1,169,247 B (`ab77964b38a3`) |
| styles.css | 129,668 B |

* بنية مختلفة (app.js أكبر بكثير، index.html أكبر) — يبدو نسخة من مرحلة أقدم (monolith).
* `adBar` = 0 → **سيفقد شغل Ad Bar** وكل ما بعد 09-17 (Responsive / Smart PDF الأحدث).
* **غير موصى به** كخيار أساسي.

---

### الخيار D — `_adbar_base/` (2026-09-20 16:12) — هجين غير متماسك

| ملف | الحجم | ملاحظة |
|---|---|---|
| index.html | 89,950 B | = الأساس القديم (لا Smart PDF UI) |
| app.js | 1,229,301 B | فيه `smartPdf`=1543, `pdfV1DelBtn`=2, `adBar`=8 |
| styles.css | 129,668 B | قديم |

index.html قديم مع app.js ضخم → **غير متوافق**؛ لا يُنصح باستخدامه كوحدة واحدة.

---

### الخيار E — `C:\EQ_RECOVERY_20260922_172553` (سجل الأمان فقط)

نسخة من **الحالة المتضررة الحالية** (91,295 / 546,139 / 190,072). لا قيمة استرجاعية — تُحفظ فقط كأثر.

---

## 3) الخلاصة

> **لا يوجد إلا خيار استرجاع واحد منطقي: A = `259d1e1` (16:20:33).**
> الخيارات C و D أقدم/غير متوافقة وستفقد شغلًا إضافيًا.

### طريقة التنفيذ المقترحة (بدون أي أمر ممنوع)

لا `git reset` · لا `git restore` · لا `git clean` · لا `git checkout` · لا حذف ملفات.

1. **نسخة حماية جديدة** من الحالة الحالية إلى `C:\EQ_PROTECT_<timestamp>` (copy فقط).
2. استخراج البايتات الدقيقة لكل ملف من الـcheckpoint:
   `git show 259d1e1:<path>` → كتابة bytes كما هي (الحفاظ على BOM لـ index.html) عبر Python.
3. الملفات الأساسية: `index.html`, `app.js`, `styles.css`.
   (واختياريًا: `src/`, `tests/`, `fonts/`, `manifest.json`, `sw.js`, `numberToWords.js`, `currencyService.js` — 707 ملف بلا `node_modules`.)
4. **لا حذف** لأي ملف موجود على القرص (الملفات الزائدة تُترك كما هي).
5. تحقق: `node --check app.js` + فتح Smart Documents + فحص `???` + فحص Console.

---

## 4) الحالة: **متوقف بانتظار اختيارك**

لم يتم تعديل أي ملف من ملفات المشروع. فقط أُنشئت ملفات تقارير مؤقتة (`_R*.py`, `_R*_*.txt`) ومجلد تحقق خارجي `C:\EQ_VERIFY_CKPT`.
